# DEV-PLAN · 利他化改造（2026-09-17）

> 起因：Eric「我还是说利他的思想，如何能让文章更好地帮助到他人，而不是追求爆款，
> 最后反而是制造垃圾」。
> 09-16 已经把评分卡从「怎么让人点进来」换成「读者带走了什么」，但**只改了闸门上的
> 那张卡**——闸门前面的供给（选题凭什么答）、闸门后面的度量（拿什么算成功）
> 都还在按旧目标跑。本次改这两头，外加补一条从来没有过的「保护读者」的红线。

## 读了哪些文档

- `skills/eric-xhs-audit/SKILL.md`（当前评分卡 35/30/20/5/5/5 与全部红线）
- `docs/20260913-写稿策略进化路径.md`（Phase 0-2 执行结论、刻度错位那次教训）
- `docs/20260916-brief.md`（产能、库存、定时任务状态）
- `scripts/xhs-loop/refine_loop.py`（选题 + 写稿 prompt）、`scripts/xhs-health/{calibrate_audit,independent_audit,draft_check}.py`
- `xhs/素材库/{横向对比宽表,发布数据,评论区原话,案例库,词库}.csv`、`必须命中清单.md`

## 事实基线（改之前先把数字钉死）

| 事实 | 数字 | 来源 |
|---|---|---|
| 真实产出 | 31 篇合计 **4716 观看 → 收藏 45 · 分享 5 · 评论 14**，收藏率 0.95% | 横向对比宽表 |
| 白看 | **14 篇收藏=0，合计 1609 观看** | 同上 |
| 主指标与价值脱钩 | 收藏最高的 8 篇，**搜索来源占比全是 0** | 同上 |
| 素材流入 | 近 14 天新增原话 **129 行**，而近 3 天 **0**（采集 exit 1 静默失败） | 评论区原话.csv / brief |
| 产能 | 写稿 loop **10 篇/天**，库存 202 篇 | brief |

**稀释率**：9.2 行新素材/天 ÷ 10 篇/天 = **每篇不到 1 行新鲜素材**。
差额只能由「正确的废话」填上——这是"制造垃圾"的生产机制，不是写手不努力。

## 改动清单（6 项，每项可独立验证）

### T1 · 素材闸门：修好坏掉的相关性判据，并把它变成选题硬闸门
**先修 bug**：`relevant_quotes/relevant_cases` 返回的 `n_rel`（"其中相关 N 条"）**是坏的**——
候选池 1148 个词，相关原话中位数 **60/60、p25 也是 60**，满预算饱和，携带零信息。
根因：`textual()` 只要**任一** 2 字片段命中就算相关，「领导」命中库里 17% 的行，
而 `_pick` 的预算只有 60 —— 松匹配的行数远超预算，`n_rel` 永远顶格。
于是 `build_prompt` 里那句 `if hit_q < 8: 素材偏薄` 警告，**从来没有真正响过**
（同一类病：审核校准按标题匹配，220 篇只配上 1 篇）。

**修法**：新增 `material_backing(kw)` —— ①按文档频率剔除高频片段（df > 10% 的丢掉，
IDF 式停用词，取代手写 `_FRAG_STOP`）②要求**≥2 个不同片段同时命中**才算相关
③不截预算、数全库。修完分布是 原话 p25=3 / p50=7 / p75=15，案例 p50=1 —— 能区分了。

**闸门**：`pick_topic` 选中词之后加判 `相关原话 ≥3 且 相关案例 ≥1`，
不过就 `log_gap(原因="素材不足")` 并跳到下一个缺口场景；兜底路径同样判，
全池都不过 → 本轮不写（这就是要的减产）。
实测放行 **703/1148 = 61%**，38 个场景里 **29 个仍有词可写**，
被判死的 9 个场景原本词池就只有 0-1 个词（已经是零产出场景），**不新增饿死**。

### T2 · 度量换锚：主指标从「搜索来源占比」改成「收藏率 + 分享」
`calibrate_audit.py` 的 `OUTCOMES` 第一位现在是搜索来源占比 —— 它测的是**渠道效率**
（被搜到了没有），跟帮没帮到人无关，而数据已经证明两者脱钩（收藏最高的 8 篇搜索占比全 0）。
改为派生 `收藏率 = 收藏/观看×100`，`OUTCOMES = ["收藏率","分享","收藏","观看","搜索来源占比","评论"]`，
「方向可疑的维度」改按新主指标判。
判据：**收藏＝我以后要用它；分享＝我要拿它去帮另一个人**，是读者被帮到时唯一会留下的痕迹。

### T3 · 新红线「照做风险」（当前红线没有一条在保护读者）
现有红线全是**保护账号**的（编造 / 身份背书 / 诱导互动 / 平台合规），
而账号教的是职场话术——照做会被打低绩效、被穿小鞋、丢 offer，**代价全由读者承担**。
新增红线：**这套话术在真实权力结构下最坏会发生什么**；最坏后果由读者承担而作者不承担的
（"当场怼回去""直接找大领导""把话挑明"这类），正文必须写明代价与适用边界，否则打回。
⚠️ 必须**三处同一次改完**（`SKILL.md` 红线表 + `independent_audit.py` 输出模板 +
`refine_loop.py` 写稿 prompt）——"写手看一张卡、审核看另一张卡"这个病本仓库犯过三次。

### T4 · 路径 B 加出处约束
09-16 新卡的路径 B（"被说中了"，情绪价值也能满分）给了模型一条逃生通道：
写不出可抄话术时转去写"精确命名处境"，而模型最擅长的恰恰是**写得像很精确**。
约束：路径 B 要满分，被命名的处境必须**指得到** `评论区原话.csv` / `案例库.csv` 里
某一条具体记录（报告里写出处），否则按 0 分判。改 SKILL.md 维度 5 + 写稿 prompt 两处。

### T5 · 删掉「留一手换评论」
`必须命中清单.md` 第 3 条修法①原文是：
> 换一类给全 —— 正文完整给出 B 或 C，**把最常见的 A 留给评论区**

这是系统里**明文规定的"故意不把东西给读者"**，为的是把评论数做上去（CES 评论权重 ×4）。
它和「读者带走了什么」直接冲突，是全仓库最反利他的一条规则。
改法：删掉「每个选项必须有回了才拿得到的东西」整条；CTA 从**必须**字母型降为**可选**
（`draft_check.py` 的编号选项检查由 issue 降为 warn）；新增判据
**「正文里该给的不许扣下来换评论」**。改 清单 + `write_actions()` + `keep_passed()` +
`draft_check.py` + SKILL.md 维度 6。

### T6 · 产能锚到素材供给（把当前这起静默故障变成熔断）
现在采集 exit 1、近 3 天素材零新增，而写稿 loop 照写 10 篇/天 —— 机器在空转，越转越稀。
新增配额：**近 14 天可写新稿数 = 近 14 天新增素材行 ÷ MATERIAL_PER_DRAFT − 近 14 天已写稿数**。
`MATERIAL_PER_DRAFT = 4`（一篇稿至少要 4 条新鲜素材作燃料）。
按当前 129 行 → 32 篇/14 天 ≈ **2.3 篇/天**（现在是 10 篇/天）。素材零新增 → 配额 0，
本轮不写，并打印采集链路的修复命令。
⚠️ **4 这个数字是选择不是拟合**，没有样本支撑它；它的作用是把产能和供给绑在一根绳上，
具体松紧 Eric 随时可调（常量 + 注释写明依据）。

## 验证方式

1. `python3 scripts/xhs-health/draft_check.py` 对存量稿回归，确认 T5 放宽后不新增违规
2. `python3 scripts/xhs-loop/refine_loop.py --dry-run` 走一遍选题，确认 T1/T6 闸门生效且不饿死
3. `python3 scripts/xhs-health/calibrate_audit.py --stdout` 确认 T2 主指标换了且报告能生成
4. 三处权重/红线文案 grep 比对，确认 T3/T4 没有再次只改一处
5. 全部 python 文件 `py_compile`

## 风险点

| 风险 | 处理 |
|---|---|
| T1 闸门把 loop 饿死 | 已实测：29/38 场景仍有词；兜底路径保留；被拦的记 `缺词信号.csv` 喂给采集 |
| T6 产能砍 4 倍，库存被发布端抽干 | 库存 202 篇、发布 5 篇/天，是**要的**结果；数字是常量，可调 |
| T5 放宽机械项引入回归 | 只把「没给编号选项」由 issue 降 warn，其余 CTA 检查（禁贵评论/禁回报承诺）全部保留 |
| T2 换主指标后同卡样本要重攒 | 不动 `CURRENT_RUBRIC_SINCE`（评分卡权重没改），只换 outcome 口径，逐篇明细照旧 |
| 又一次「只改一处」 | T3/T4 的验收项 4 就是为此设的 |

## 不在本次范围

- 不动评分卡权重（09-16 刚换，样本要重攒，本仓库规矩是样本不够不动权重）
- 不动发布配额与定时任务链路
- 采集 exit 1 的根因不在本次修（T6 把它变成可见的熔断，修它是另一条线）

---

## 执行状态

| 任务 | 状态 |
|---|---|
| T1 素材闸门 | ✅ 完成 |
| T2 度量换锚 | ✅ 完成 |
| T3 照做风险红线 | ✅ 完成 |
| T4 路径B 出处约束 | ✅ 完成 |
| T5 删留一手换评论 | ✅ 完成 |
| T6 产能锚素材 | ✅ 完成 |
| 验证 | ✅ 完成 |
