# DEV-PLAN · 旋钮配置台（2026-09-17）

> 起因：Eric「能不能把这些规则、条件都做成一个 html 版本的配置页面，我可以调这些旋钮」。

## 一个必须先说清的约束

**托管在 claude.ai 上的页面驱动不了这条流水线。** 写稿/采集/审核跑在本机 launchd 上
（09:00 采集、12:30 探测、17:30 审核…），它们读的是本地文件。一个云端页面改了值，
没有任何东西会把它拉下来——那就变成「等人来同步」的环节，而这类环节的实际执行率是零。

所以配置台做成**本地页面 + 本地小服务**：页面直接写本地 JSON，脚本直接读同一个 JSON。
改完即生效，下一轮定时任务就按新值跑，中间没有人。

## 为什么值得做（不只是为了好看）

盘点出 25 个散在 6 个文件里的常量。其中 **`PASS_SCORE` 在两个文件里各定义了一次**，
而 `independent_audit.py` 的注释白纸黑字写着「必须与 refine_loop.PASS_SCORE 保持同一个数，
两处不同步会重演 2026-08-05 那个 bug」——靠注释提醒人手动同步。
同一个病这个仓库已经犯过四次（评分卡权重 vs 写稿 prompt、审核输出模板、CTA、PASS_SCORE），
每次都是「改了一处漏了另一处」，最长漏了一个月。
**集中成一份配置，这类 bug 在结构上就不可能再发生**——这比配置页本身值钱。

## 模块拆解

### 1. `scripts/knobs.py` —— 单一真相源（新模块）
- `SCHEMA`：每个旋钮的 **默认值 / 范围 / 单位 / 一句话说明 / 依据 / 影响哪条链路**。
  依据字段是硬要求：这个仓库的规矩是「样本不够不动权重」，旋钮页必须把
  「这个数是实测标定的」还是「这是拍板选的」当面写清楚，否则调的人会以为都一样可靠。
- `config/knobs.json`：只存**改动过的值**（overrides），默认值留在代码里。
  这样 JSON 小、diff 干净，且删掉文件就回到默认。
- `K("MATERIAL_PER_DRAFT")`：读取 + 范围校验，越界拒绝并回退默认 + 告警。

### 2. 接线（把常量换成 `K(...)`）
按「会不会真去调」排优先级，第一批 12 个：

| 旋钮 | 现值 | 管什么 |
|---|---|---|
| `MATERIAL_PER_DRAFT` | 25 | 一篇稿吃多少条素材 → **直接决定产能** |
| `MATERIAL_PER_DAY` | 4 | 每日写稿封顶 |
| `MATERIAL_WINDOW_DAYS` | 14 | 配额窗口 |
| `MATERIAL_MIN_CASES` | 1 | 选题闸门：至少几条案例 |
| `MIN_SLOT_STRENGTH` | 1.0 | 搜索位日均赞下限 |
| `PASS_SCORE` | 80 | 过线分（**消灭两处重复定义**） |
| `MAX_ROUNDS` | 4 | 返工轮数上限 |
| `STOCK_TARGET` | 50 | 库存目标 |
| `BLESSING_PER_WEEK` | 2 | 每周祝福流篇数 |
| `QUOTA_DAYS` | 14 | 场景配额窗口 |
| `TITLE_DUP_THRESHOLD` | 0.50 | 标题查重线 |
| `MAX_AVG_SENT_LEN` | 30 | 平均句长上限 |

⛔ 不接线的：`EXIT_*`、`*_COL_INDEX`、`LOG_COLUMNS` 这类**不是旋钮是契约**，
调了会直接把链路弄坏，不该出现在页面上。

### 3. `scripts/knobs_server.py` —— 本地服务（零依赖，标准库 http.server）
- `GET /` → 配置页　`GET /api/knobs` → 当前值+schema　`PUT /api/knobs` → 落盘
- 落盘走**原子替换**（tmp + replace），并追加一行到 `config/knobs-changelog.csv`：
  时间 / 旋钮 / 旧值 / 新值 / 备注。这个仓库反复吃过「改了没人知道为什么」的亏。
- 只监听 `127.0.0.1`。不加认证，因为它只对本机开。

### 4. `config/knobs.html` —— 配置页
- 按链路分组：**产能 / 选题闸门 / 审核 / 写稿规格 / 采集**
- 每个旋钮：滑块 + 数字框、范围、当前值 vs 默认值（改过的高亮）、说明、**依据**
- 🎯 **实时后果预估**（这是这个页面真正的价值，不是好看）：
  调 `MATERIAL_PER_DRAFT` 时当场算出「按近 14 天采集速率 → 预计 X 篇/天」，
  调 `PASS_SCORE` 时当场算出「按历史 62 篇审核分布 → 会放行 X 篇 / 拦下 X 篇」。
  让人看见代价再动手，而不是调完两周后才发现产能塌了。
- 「恢复默认」按钮；改动未保存时有明显标记

### 5. `com.eric.xhsknobs.plist` —— 开机常驻（可选，先不装）

## 验证方式

1. `K()` 单测：越界拒绝、缺 JSON 回退默认、类型错误不崩
2. 改一个值 → 落盘 → `refine_loop --dry-run` 读到新值（端到端）
3. `PASS_SCORE` 两处统一后，refine_loop 与 independent_audit 读到同一个数
4. changelog 有记录
5. 全部脚本 py_compile + 现有验证（draft_check --regress / health_check）不回归

## 风险点

| 风险 | 处理 |
|---|---|
| 调错把流水线调坏 | 每个旋钮有范围校验；越界回退默认并告警；changelog 可回溯 |
| 配置漂移（有人直接改代码常量） | 接线后代码里不再留裸常量，只留 `K()` 与 SCHEMA 默认值 |
| 本地服务没人开，页面形同虚设 | ~~先做成一条命令；跑顺了再上 launchd~~ **这条当天就兑现了**：手动起的进程随会话一起没了，Eric 点保存只拿到一句「保存失败」。已上 `com.eric.xhsknobs`（KeepAlive 常驻），页面也会直说「服务没在跑」而不是含糊报错 |
| 页面误导：把「拍板值」当「实测值」调 | SCHEMA 强制写 `依据` 字段，页面上直接显示「实测标定/拍板选定」 |

## 不在本次范围

- 不改任何旋钮的**当前值**（只是把它们挪到可调的地方，行为零变化）
- 不接线那 13 个契约型常量
- 不做云端镜像（理由见开头）

---

## 请确认

1. **本地页面**（能真正驱动流水线）这个方向对不对？
2. 第一批 12 个旋钮的选择，有没有该加/该去掉的？
3. 「实时后果预估」要不要做（它最费工，但也是这页最值钱的部分）？
