--- /private/tmp/claude-501/-Volumes-datacenter-04-eric-AIcoding-businessskills/a2657c9c-cfcc-4e53-b4f9-6fdf17fce3d2/scratchpad/sync-worktree/skills/eric-upgrade/SKILL.md	2026-07-30 22:16:47
+++ /var/folders/b_/rvnfgp8159q1x74n0x5675r00000gn/T/tmp.yIFzD5YNF0	2026-07-30 22:17:01
@@ -1,132 +1,57 @@
 ---
-name: eric-upgrade
-description: 升级 eric 到最新版本
-trigger: /eric-upgrade、/升级eric、「升级 eric」
+name: eric-update
+description: |
+  eric 更新器。用户说「更新 eric」「升级 eric」「把 eric 更新到最新版」「检查 eric 更新」或输入 /eric-update 时使用。只同步 dontbesilent2025/eric，不更新用户安装的其他 Skill，不修改用户的 eric 存档。
+  Update eric when the user asks to update, upgrade, or check updates for eric.
 ---
 
-# eric-upgrade
+# eric-update：更新 eric
 
-升级 eric 到最新版本，显示更新内容。
+用户已经明确要求更新 eric。直接执行更新，不再做第二次文字确认；宿主若要求 Shell 权限，由用户在宿主的权限窗口中决定。
 
-## 使用场景
+## 更新范围
 
-- 用户主动调用 `/eric-upgrade` 升级
-- 显示版本变化和更新内容
+- 只更新官方仓库 `dontbesilent2025/eric`。
+- 保留用户在 `~/.dbs/` 中的存档、报告和决策记录。
+- 不更新用户安装的其他 Skill。
+- 不创建后台任务、定时任务或 Agent Hook。
 
-## 升级流程
+## 执行步骤
 
-### Step 1: 检测安装位置
+1. 运行以下命令，同步官方 eric 的全部正式 Skill 到已支持的 Agent：
 
-```bash
-if [ -d "$HOME/.claude/skills/dbs" ]; then
-  INSTALL_DIR="$HOME/.claude/skills"
-  echo "Install location: $INSTALL_DIR"
-else
-  echo "ERROR: eric not found in ~/.claude/skills/"
-  exit 1
-fi
-```
+   ```bash
+   npx -y skills add dontbesilent2025/eric -g --all
+   ```
 
-### Step 2: 获取当前版本
+2. 命令成功后，告诉用户更新已完成。当前 Agent 若没有立即重新读取 Skill，提醒用户新建一次对话后再使用新能力。
 
-```bash
-OLD_VERSION=$(cat "$HOME/.claude/skills/eric-upgrade/../../VERSION" 2>/dev/null || echo "unknown")
-echo "Current version: $OLD_VERSION"
-```
+3. 命令失败时，用一句话说明失败原因和下一步需要用户处理的权限或网络问题。不要把完整终端日志直接贴给用户，除非用户要求。
 
-### Step 3: 获取远程版本
+## 回复格式
 
-```bash
-REMOTE_VERSION=$(curl -sL https://raw.githubusercontent.com/dontbesilent2025/eric/main/VERSION || echo "")
-if [ -z "$REMOTE_VERSION" ]; then
-  echo "ERROR: Cannot fetch remote version"
-  exit 1
-fi
-echo "Remote version: $REMOTE_VERSION"
-```
+成功：
 
-### Step 4: 比较版本
+> eric 已更新完成。当前对话如果还没有读取到新能力，新建一次对话后即可使用。
 
-如果 `OLD_VERSION` 等于 `REMOTE_VERSION`，告诉用户已是最新版本，结束。
+失败：
 
-否则继续升级。
+> eric 没有更新完成：{简短原因}。处理完 {权限或网络问题} 后，再说一次「更新 eric」。
 
-### Step 5: 备份当前版本
+## 边界
 
-```bash
-BACKUP_DIR="$HOME/.claude/skills/.eric-backup-$(date +%Y%m%d-%H%M%S)"
-mkdir -p "$BACKUP_DIR"
-cp -r "$HOME/.claude/skills"/dbs* "$BACKUP_DIR/" 2>/dev/null || true
-echo "Backup created: $BACKUP_DIR"
-```
+- 用户只问版本、更新内容或是否需要更新时，先回答问题，不执行命令。
+- 用户明确要求检查更新且希望实际同步时，按本 Skill 更新。
+- 不使用 `npx skills update`，该命令可能更新用户安装的其他 Skill。
 
-### Step 6: 下载最新版本
+---
 
-```bash
-TMP_DIR=$(mktemp -d)
-git clone --depth 1 https://github.com/dontbesilent2025/eric.git "$TMP_DIR/eric"
-if [ $? -ne 0 ]; then
-  echo "ERROR: Failed to clone repository"
-  exit 1
-fi
-echo "Downloaded to: $TMP_DIR/eric"
-```
+## 不知道下一步用哪个 Skill？
 
-### Step 7: 替换旧版本
+更新任务完成、当前 Agent 已经重新读取新能力后，输入 `/dbs`。
 
-```bash
-rm -rf "$HOME/.claude/skills"/dbs*
-cp -r "$TMP_DIR/eric/skills"/dbs* "$HOME/.claude/skills/"
-rm -rf "$TMP_DIR"
-echo "Upgrade completed"
-```
+这是商业工具箱的导航入口。它会读取刚才的具体结论和你的最新目标，选择当前最值得处理的一个方向，并直接路由到对应 Skill。
 
-如果复制失败，从备份恢复：
+你也可以直接说你想做什么。`/dbs` 会尊重你的明确选择。
 
-```bash
-if [ $? -ne 0 ]; then
-  echo "ERROR: Upgrade failed, restoring from backup..."
-  rm -rf "$HOME/.claude/skills"/dbs*
-  cp -r "$BACKUP_DIR"/* "$HOME/.claude/skills/"
-  echo "Restored from backup"
-  exit 1
-fi
-```
-
-### Step 8: 显示更新内容
-
-读取 `$HOME/.claude/skills/dbs/../../README.md`（如果存在），提取从 `OLD_VERSION` 到 `REMOTE_VERSION` 之间的更新内容。
-
-格式：
-
-```
-eric v{REMOTE_VERSION} — 从 v{OLD_VERSION} 升级成功！
-
-更新内容：
-- [从 README 提取的更新要点]
-
-升级完成！
-```
-
-### Step 9: 清理备份
-
-询问用户是否删除备份：
-
-```bash
-echo "Backup location: $BACKUP_DIR"
-echo "Keep backup? (will be auto-deleted in 7 days if not used)"
-```
-
-不强制删除，让用户自己决定。
-
-## 错误处理
-
-- 网络失败：提示用户检查网络连接
-- Git clone 失败：从备份恢复
-- 文件复制失败：从备份恢复
-
-## 注意事项
-
-- 只支持通过 `~/.claude/skills/` 安装的版本
-- 升级前自动备份，失败时自动恢复
-- 不需要用户手动操作 git
+不熟悉所有 Skill 没关系，下一步不确定时就回 `/dbs`。
