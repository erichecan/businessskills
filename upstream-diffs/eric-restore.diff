--- /Volumes/datacenter/04-eric/AIcoding/businessskills/skills/eric-restore/SKILL.md	2026-07-30 22:55:52
+++ /var/folders/b_/rvnfgp8159q1x74n0x5675r00000gn/T/tmp.JytHTL36k8	2026-08-07 22:26:38
@@ -1,10 +1,6 @@
 ---
 name: eric-restore
-description: |
-  把上次诊断的状态拉出来，接着用。配合 eric-save 使用。
-  触发方式：/eric-restore、/续上、「接着上次」「之前的结论」「上次诊断到哪了」
-  Restore the most recent diagnosis snapshot saved by eric-save.
-  Trigger: /eric-restore, "continue from last time", "where did we leave off"
+description: 恢复由 eric-save 保存的最近诊断状态。用户要求接着上次、查看此前结论或继续未完成诊断时使用。
 ---
 
 # eric-restore：接续诊断
@@ -96,7 +92,7 @@
 ```
 当前存档位置没有诊断记录：{存档根目录}
 
-输入 `/eric` 开始处理一个真实任务；形成结论后，明确说「保存」即可建立可恢复记录。以前改过存档位置时，可以输入 `/eric-save location` 查看当前设置。
+输入 `/dbs` 开始处理一个真实任务；形成结论后，明确说「保存」即可建立可恢复记录。以前改过存档位置时，可以输入 `/eric-save location` 查看当前设置。
 ```
 
 **情况 B：list 模式**
@@ -135,7 +131,7 @@
 - {假设 1}（如果没有就写「（暂无）」）
 
 ### 上次留的下一步
-{已确认下一步原文；没有就写「待定，交回 /eric 判断」}
+{已确认下一步原文；没有就写「待定，交回 /dbs 判断」}
 
 ---
 
@@ -158,7 +154,7 @@
 
 **B. 用户说「我有新情况」/「之前那个不重要了」**
 
-→ 把新情况交回 `/eric`，按主路由重新判断。说一句：
+→ 把新情况交回 `/dbs`，按主路由重新判断。说一句：
 
 > 那把新情况说一下，从头来。
 
@@ -166,7 +162,7 @@
 
 **C. 用户说具体的新问题**
 
-→ 按 eric 主路由的逻辑判断该走哪个 skill。
+→ 按 dbs 主路由的逻辑判断该走哪个 skill。
 
 ---
 
@@ -193,8 +189,8 @@
 | 用户回应 | 路由到 |
 |---|---|
 | 「按 next_skill 接着走」/「按上次确认的继续」 | `/<next_skill 字段值>`，并把存档内容作为上下文 |
-| 「我换个问题」/「重新开始」 | 交回 `/eric`，按新目标重新判断 |
-| 提到具体的新需求（小红书标题、AI 检测等） | 按 eric 主路由判断，路由到对应 skill |
+| 「我换个问题」/「重新开始」 | 交回 `/dbs`，按新目标重新判断 |
+| 提到具体的新需求（小红书标题、AI 检测等） | 按 dbs 主路由判断，路由到对应 skill |
 
 ---
 
@@ -208,10 +204,10 @@
 
 ## 不知道下一步用哪个 Skill？
 
-输入 `/eric`。
+输入 `/dbs`。
 
 这是商业工具箱的导航入口。它会读取刚才的具体结论和你的最新目标，选择当前最值得处理的一个方向，并直接路由到对应 Skill。
 
-你也可以直接说你想做什么。`/eric` 会尊重你的明确选择。
+你也可以直接说你想做什么。`/dbs` 会尊重你的明确选择。
 
-不熟悉所有 Skill 没关系，下一步不确定时就回 `/eric`。
+不熟悉所有 Skill 没关系，下一步不确定时就回 `/dbs`。
