# 任务提示词改进条款（请粘贴进 SKILL.md / 任务提示词）

> 我（助手）无法从当前会话编辑你保存的 skill（只读缓存）。请到 **设置 > Capabilities** 打开该任务，把下面 4 段粘进对应位置。粘贴后，未来每天的自动运行才会真正执行日志与告警逻辑。
> `运行日志.csv` 已创建并回填（6/24、6/25 各轮），放在固定文件夹。

---

## 条款 A —— 运行日志 + 连续 0 新增告警（加到「第 0 步：读取持久记忆」和「第 6 步：交付」）

第 0 步追加读取：

```
- 运行日志.csv：列为 日期 | 轮次 | 跑的关键词数 | 总抓取条数(估) | 本轮新增条数 | 记忆库累计 | 小红书状态 | 连续0新增轮数 | 告警 | 备注
  读到后看最近一行的「连续0新增轮数」。
```

第 6 步收尾追加（写日志 + 告警）：

```
- 在 运行日志.csv 末尾追加本轮一行：填本轮跑的关键词数、抓取条数、本轮新增条数、更新后的记忆库累计行数、小红书状态、备注。
- 计算「连续0新增轮数」：若本轮新增=0，则取上一行该值+1，否则置0。
- 告警规则：当「连续0新增轮数」≥2，本轮「告警」列填「是」，并在交付总结开头用一句醒目话提示：
  「⚠️ 已连续 N 轮 0 新增，疑似空转——可能小红书未登录/被验证码全拦、关键词全部饱和、或去重误杀，请排查。」
  否则「告警」列填「否」。
```

## 条款 B —— 去重改用复合键（加到「第 2 步：逐个关键词收集」）

```
- 去重不要只用标题字符串。先把标题做规范化：全角→半角(（）！，：等)、去除所有 emoji 与多余空格，再与「作者」拼成复合键「规范化标题|作者」作为去重依据。
- 同一篇笔记标题在不同账号转载视为不同条目可保留；同账号同标题判为重复跳过。
- 已知历史里有约 19 组仅标点/全半角差异的重复标题，规范化键可消除此类漏网。
```

## 条款 C —— 增加可排序的热度数值列（加到记忆库与 Excel 列定义）

```
- 记忆库与 Excel 内容表在「热度」(原始字符串,如 10.6万/504/—) 之外，新增一列「热度数值」：
  把 "X万" 解析为 X*10000、纯数字原样、"—"/未取到 记为 空(留空,不要填0)。
- 排序/筛选高低热一律用「热度数值」列。
- 注意：改记忆库列数后，后续所有写入都必须按新列数(7列)写，避免与历史 6 列错位；
  历史行的「热度数值」可一次性回填。
```

## 条款 D —— 优先抓取笔记详情页链接（加到「第 2 步」小红书部分）

```
- 当前链接多为搜索页URL(search_result?keyword=...)，点回去仍需再找,定位差。
- 改进：读取搜索结果后，对要收录的笔记用 find/read_page 拿到其单篇详情链接
  (形如 https://www.xiaohongshu.com/explore/<noteId> 或 /discovery/item/<noteId>)，
  链接列优先存详情页URL；确实取不到再退回搜索页URL，并在备注标注「仅搜索页」。
- 为控制验证码风险，可只对每个关键词的「高赞精读对象(2高1低)」抓详情链接，列表其余条目允许暂存搜索页URL。
```

---

## 现状核对结论（供参考，2026-06-25 核实）
- 小红书链接：198/198 为搜索页URL，0 详情页 → 条款 D。
- 去重：规范化后 19 组重复标题 → 条款 B。
- 热度：列为字符串、无数值列 → 条款 C。
- 「84条停在6/24/多日空转」：不成立。CSV 解析为 6/24=84、6/25=208、合计292；文件 0 坏行、热度 0 异常值。仅 2 天历史，无空转。告警仍建议加作前瞻防护 → 条款 A。
