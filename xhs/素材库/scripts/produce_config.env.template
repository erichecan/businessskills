# ============================================================
# 视频生产流水线 · 配置文件
# 复制为 produce_config.env，填入真实值，不要提交到 Git
# ============================================================

# Fish Audio —— 克隆人声 TTS
FISH_API_KEY=your_fish_audio_api_key_here
FISH_VOICE_ID=your_cloned_voice_model_id_here   # Fish Audio 控制台 > 我的声音 > 复制 ID

# HyperFrame CLI —— 视频画面生成
# 如果 hyperframe 不在 PATH，填写绝对路径
HYPERFRAME_BIN=hyperframe

# 输出目录（视频、音频成品存放位置）
VIDEO_OUTPUT_DIR=/Volumes/datacenter/04-eric/AIcoding/businessskills/xhs/素材库/成品视频
