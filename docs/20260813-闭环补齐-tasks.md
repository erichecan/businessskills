# 20260813 · 闭环补齐 + 积压消化台账

Eric 2026-08-13 定的顺序：**先做方案 3（跑通对账）→ 再做方案 1（最小反哺闭环）
→ 消化 36 篇返工 → 用 63 个已验证词测 Gemini 写稿能力。**

## 起点事实（08-13 凌晨实测，不是估计）

- 数据 review **今天才通**：08-12 修好 `fetch_stats` 补上满 7 天数据后，
  对账当天产出 14 条复盘。此前 41 条预测、复盘 0 条，因为没有任何 ≥7 天的数据行。
- 复盘产出质量高于预期：能证伪具体假设、定位根因、给出可执行改法，
  且守得住「样本不足不动系数」的边界。
- **反哺仍然断**：`预测复盘.md` 全仓库只有 `review_prediction.py` 自己读写；
  `知识框架.md`（写稿 prompt 的大脑）只被读、从没被任何脚本写过。
- 积压：36 篇待返工（7 篇 84 分、3 篇 83 分）· 63 个已验证词未写稿 · 待分析 probe 0。

## 任务

- [ ] T13 让 `review_prediction.py` 真正可持续跑（＝方案 3）
      三处一起改，缺一条都不算跑通：
      a) 修重复复盘：同一篇稿在 `预测记录.csv` 里有多条预测（每次返工过线写一条），
         `todo` 在循环前一次性算好，于是同一篇被复盘多次 —— 实测 14 条里 5 条重复
         （08-08_汇报被打断 ×4、08-11_汇报被打断 ×3）。每条都调一次 claude，
         且会让「攒够 5 篇再改系数」的样本计数虚高。
      b) 接 Gemini 首轮 + Claude 回退（与 auto_analyze / refine_loop 同一套）
      c) 补 `HEADLESS_FLAGS`：现在是裸调 `claude -p`，不禁工具，每次多背约 7k token；
         也没有 `claude_limits` 处理，撞额度直接失败不重试
      验收：同一批数据重跑，复盘条数 = 不重复的可复盘稿数；日志显示由 Gemini 分析

- [ ] T14 最小反哺闭环（＝方案 1）
      把复盘结论沉淀成写稿 prompt 能读的先验，而不是躺在 md 里没人看。
      验收：写稿 prompt 里出现来自复盘的先验；`refine_loop` 读得到；不改判据只加信息

- [ ] T15 消化 36 篇返工积压（走 Claude，验证 08-12 加的字数余量提示）
      验收：过线篇数与失败原因分布；确认第 2/3 轮不再因字数超标白跑

- [ ] T16 用 63 个已验证词测 Gemini 首轮写稿能力
      验收：≥5 篇的分数分布、红线命中类型；与历史 Claude 首轮分数对照
      重点看 08-12 那两个已知弱点是否复现：引语改写触红线、卡片文案偏长

## 已知不动的边界

- 互动侧系数（`LIKE_RATE`、收藏/评论/转发占比）本轮不改 —— 复盘自己写了
  「5 项全命中且只有 1 篇样本，要改需再攒 4 篇」。修完重复 bug 后样本计数才准。
- 返工仍走 Claude，不换 Gemini（Eric 08-12 定的分工，且 Gemini 免费层只有 Flash）。
