# 20260813 · 闭环补齐 + 积压消化台账

Eric 2026-08-13 定的顺序：**先做方案 3（跑通对账）→ 再做方案 1（最小反哺闭环）
→ 消化 36 篇返工 → 用 63 个已验证词测 Gemini 写稿能力。**

## 起点事实（08-13 凌晨实测，不是估计）

- 数据 review **今天才通**：08-12 修好 `fetch_stats` 补上满 7 天数据后，
  对账当天产出 14 条复盘。此前 41 条预测、复盘 0 条，因为没有任何 ≥7 天的数据行。
- 复盘产出质量高于预期：能证伪具体假设、定位根因、给出可执行改法，
  且守得住「样本不足不动系数」的边界。
- **反哺仍然断**：`预测复盘.md` 全仓库只有 `review_prediction.py` 自己读写；
  `知识框架.md`（写稿 prompt 的大脑）只被读、从没被任何脚本写过。
- 积压：36 篇待返工（7 篇 84 分、3 篇 83 分）· 63 个已验证词未写稿 · 待分析 probe 0。

## 任务

- [x] T13 让 `review_prediction.py` 真正可持续跑（＝方案 3）
      三处一起改，缺一条都不算跑通：
      a) 修重复复盘：同一篇稿在 `预测记录.csv` 里有多条预测（每次返工过线写一条），
         `todo` 在循环前一次性算好，于是同一篇被复盘多次 —— 实测 14 条里 5 条重复
         （08-08_汇报被打断 ×4、08-11_汇报被打断 ×3）。每条都调一次 claude，
         且会让「攒够 5 篇再改系数」的样本计数虚高。
      b) 接 Gemini 首轮 + Claude 回退（与 auto_analyze / refine_loop 同一套）
      c) 补 `HEADLESS_FLAGS`：现在是裸调 `claude -p`，不禁工具，每次多背约 7k token；
         也没有 `claude_limits` 处理，撞额度直接失败不重试
      验收：✅ 41 条预测→32 篇稿，一轮省 9 次调用；存量 14 条复盘去重为 9 条（备份 .bak-0813）；
            Gemini 归因实测通过，输出指名假设+数字作证+守住「1 篇样本不改系数」的边界

- [x] T14 最小反哺闭环（＝方案 1）
      落点改了：复盘结论讲的是**预测模型系数**，不是写稿手法，所以通道建在
      `review_prediction → 预测校准.csv → predict.py`，而不是塞进写稿 prompt。
      （选题闸门 08-12 已用日均赞分档，那一侧不缺信息。）
      md 存归因（自然语言，会变），csv 存代码算出的偏差数字（是事实）——
      不让 predict 去 parse md，那等于把「模型写了什么」当判据。
      验收：✅ `calibration_hint()` 输出「密度低 实际/预测中位 = 0.24×，样本 1 篇 < 5，
            不足以改系数」——只提示不自动改，样本够了仍由人拍板

      ⚠️ 回填时发现 T13 的去重**不彻底**：真正的重复是「多个成稿对应同一个笔记」。
      光「汇报被领导打断怎么接」就重写过 6 篇稿、只有一篇真发布，6 篇全部对账到
      同一份实际数据。按文件名去重 = 把 1 个样本记成 6 个，「≥5 篇才改系数」当场失守。
      去重键已改为 noteId：**9 篇 → 2 个真实样本**。

- [⛔] T15 消化 36 篇返工积压 —— **跑了 11 篇，过线 0，判定为负收益，暂停**
      按第三节「同一个问题连续 2 次没修好就停下来问」的规矩，停在这里。

      **实测数据（11 篇，两轮尝试）**

      第一轮 8 篇（加约束前）：过线 0。三篇有完整记录的：
        汇报被打断分三种 84 → 轮1 **71**、轮2 字数565✗ → 回滚 84
        汇报被打断未必怪你 84 → 轮1 字数602✗、轮2 **78** → 回滚 84
        汇报被打断       83 → 轮1 字数623✗、轮2 **80** → 回滚 83

      第二轮 3 篇（加了字数守恒硬约束后）：过线 0，**没有改善**：
        汇报被打断   84 → 轮1 **78**、轮2 字数**643**✗（比加约束前的 623 还高）
        绩效被打低分 84 → 轮1 **68**、轮2 字数599✗
        领导不回消息 84 → 轮1 **68**、轮2 **81**

      **两个症状，一个根因**
      症状一：返工时正文净增 90-159 字（原文的 19-34%）——
              464→623 · 469→602 · 475→565，这不是微调是重写。
      症状二：**每一轮都降分，11 篇无一例外**（起点 83-84，返工后 68-81）。
      根因：模型没在做定向修改。prompt 早写着「只改点到的问题，其余保持原样，
      不要重写」，它照样重写；重写把原稿里本来得分的部分一起换掉了。

      **08-12 那版字数提示是失效的**：按余量分档（>80 字只报数字），
      而这批稿返工前余量 85-96 字，刚好越过阈值，一条约束都没给。
      已改成每次都给硬区间（「最多只准多 30 字」）——但第二轮证明**改 prompt 解决不了**。

      **待排除的更根本可能：审核分数本身不稳**
      「领导不回消息」今天 4 次审核（返工的不同版本）：64 / 76 / 68 / 81，跨度 17 分，
      其中标题项 10 / 16 / 11 / 20。若同一份内容重审也差这么多，
      那 84 vs 85 的区分就是噪声，所有返工都在追噪声。
      正在做「同一份内容审两次」的对照实验（内容指纹 5ab81cc5…）。

      **给 Eric 的建议**：83+ 的稿单独归一类，停止自动返工
      （36 篇里有 10 篇在这个区间），转人工微调或直接放行。

- [ ] T16 用 63 个已验证词测 Gemini 首轮写稿能力
      验收：≥5 篇的分数分布、红线命中类型；与历史 Claude 首轮分数对照
      重点看 08-12 那两个已知弱点是否复现：引语改写触红线、卡片文案偏长

## 已知不动的边界

- 互动侧系数（`LIKE_RATE`、收藏/评论/转发占比）本轮不改 —— 复盘自己写了
  「5 项全命中且只有 1 篇样本，要改需再攒 4 篇」。修完重复 bug 后样本计数才准。
- 返工仍走 Claude，不换 Gemini（Eric 08-12 定的分工，且 Gemini 免费层只有 Flash）。
