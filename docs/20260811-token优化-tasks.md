# Token 消耗优化 · 任务台账（2026-08-11 起）

依据：`docs/20260811-token消耗测算与优化.md`（实测 469 个会话 jsonl）。
基线：自动化日成本 $127，周额度 $1,350–1,500，每周瘫 1–2 天。
目标：自动化日成本 → $45 以内，不再出现整天停摆。

**台账是进度的唯一真相。每完成一条：验证 → 提交 → 回写 `[x] + commit hash`。**

---

## T1 撞 weekly limit 立刻停手，不进 5 小时重试循环
- [x] 完成 · `d1f4b3e` · 实测：改前撞 weekly 时 sleep 10 次（5 小时），改后 0 次
- **问题**：`refine_loop.py:468` 和 `independent_audit.py:87` 的 `LIMIT_RE` 不区分
  session limit（5h 窗口，该等）和 weekly limit（要等到周日，不该等）。
  实测 08-10 全天 112 次调用、0 token、0 产出，就是每次都熬满 5 小时。
- **验收**：
  1. 喂入含 `You've hit your weekly limit · resets 12am` 的假输出 → 函数立即返回，
     `time.sleep` 零调用
  2. 喂入含 `You've hit your session limit · resets 4am` 的假输出 → 仍走等待路径
  3. 两个脚本行为一致
- **产出**：`scripts/xhs-loop/refine_loop.py`、`scripts/xhs-health/independent_audit.py`、
  新增 `scripts/test_limit_handling.py`（放 scripts/ 根，三个调用点共用）
- **依赖**：无

## T2 审核 prompt 按成稿反查筛料
- [x] 完成 · `671c06c` · 实测 prompt 121,578→24,557 字（−80%）、写缓存 250k→73k（−71%）、
  单次 $2.99→$1.13（−62%）。两篇端到端重审：88→86、75→72，可信度维度 +1/0 未退化。
  顺带修掉真实误判源：probe 探测结果的 quotes 此前从未喂给审核员，
  而成稿原话多来自那里（试用期没结果.md 对原话库强命中为 0）
- **问题**：审核 prompt 148k 字，其中三个整库占 89%（评论区原话 70.8k / 词库 33.9k /
  案例库 26.6k）。审核是**核对**不是**选材**——只需验证成稿里出现的原话在不在库里。
- **改法**：从成稿正文反查（最长公共子串 ≥6 字 = 照抄命中，4–5 字 = 疑似改写），
  强命中全留，弱命中补满预算，词库只给本篇关键词那一行。
- **验收**：
  1. 成稿里照抄的原话，**100% 出现在筛后料包里**（否则审核员会把"库里没有"
     误判成"编造原话"扣可信度分——这是最大风险）
  2. prompt 字数 148k → 40k 以下
  3. prompt 里如实声明这是筛过的子集及筛选规则（⛔ 不得谎报为整库）
  4. 同一篇稿筛前/筛后各审一次，总分差 ≤3 分
- **产出**：`scripts/xhs-health/independent_audit.py`、`scripts/xhs-health/test_audit_pack.py`
- **依赖**：无

## T3 probe / auto_analyze 的限额处理对齐
- [x] 完成 · `d1f4b3e` · auto_analyze 此前**完全没有**额度处理，撞额度被当成
  「分析失败」丢弃任务；现已抽出 run_claude 走统一判定
- **问题**：`auto_analyze.py` 也调 claude，需确认是否有同样的 weekly limit 空转问题。
- **验收**：三个调用点用同一套限额判定；weekly 立即停、session 才等
- **产出**：`scripts/xhs-probe/auto_analyze.py`
- **依赖**：T1（复用同一个判定函数）

## T4 审核 / probe 改走 Messages API 直调
- [~] **不做**（2026-08-11 判定，理由见下，非「未完成」）
- **问题**：审核 prompt 约 100k token，实测写缓存 311k token——多出来的是
  Claude Code CLI 注入的 system prompt、工具定义、skills 列表，对单轮纯文本任务无用。
  且 CLI 用 1h TTL 缓存（2× 价），而审核读/写缓存比只有 0.02，等于付 2 倍价钱写了没人读的缓存。
- **验收**：
  1. 同一 prompt 直调 vs CLI，输出格式一致、可解析
  2. 实测写缓存 token 下降 ≥50%
  3. 保留限额重试语义（429 / rate_limit_error）
- **产出**：`scripts/lib/claude_api.py`（共用）、`independent_audit.py`
- **不做的两个理由**：
  1. **前提不成立**：`ANTHROPIC_API_KEY` / `ANTHROPIC_AUTH_TOKEN` 均未设置，
     `ant` CLI 未安装，项目里也没有任何可用凭证 —— 触发台账硬停止条件
     「需要外部凭证而项目里没有」。
  2. **经济账是反的**：本文档里的「$」全是 **API 等价成本**，用于衡量额度消耗，
     不是现金支出 —— 这些消耗由 Claude Code 订阅覆盖，月费已付。改走 Messages API
     需要独立的按量付费 API key，$1.13/次会**变成真实账单**。
     按改造后 189/周 估，一年约 $9,800 的真金白银，去换一个订阅已经覆盖的能力。
- **替代方案已生效**：T2 + T5 把成本降到额度以内（见下方结论），
  周额度压力已解除，不再需要绕开它。若将来产能大幅上调再重新评估。
- **依赖**：T1、T2

## T5 写稿把机械检查移出会话 →（实为「禁用工具 + 空 cwd」）
- [x] 完成 · `9a97876` · 请求/次 24→2、工具/次 14→0、读缓存 −98%、单次 $4.58→$2.06（−52%）
- **原假设是错的**：机械检查早就在会话外跑（refine_loop:578）。模型那 14 次 Bash
  是在**找路**（85× cd scripts/xhs-health、7× 读 draft_check.py 源码），
  诱因是 prompt 里「改完自己数一遍」。修法改为全禁工具 + prompt 明说没有工具。
- **问题**：写稿单次 24 次请求 / 14 次 Bash，prompt 砍 58% 成本只降 18%，
  钱在会话内工具往返累积的上下文。
- **验收**：请求数/次 24 → ≤8；成稿质量（独立审核分）不低于改前基线
- **产出**：`scripts/xhs-loop/refine_loop.py`
- **依赖**：T2（先确认审核链路稳定，才能用它衡量写稿质量没退化）

## T6 产能与发布配额对齐
- [x] 完成 · `66075fc` · 8 篇/天 → 4 篇/天，过剩比 2.67 → 1.33
- **问题**：`auto_publish.py:74` `DAILY_QUOTA=3`，写稿仍 2 次/天 ×4 篇 = 8 篇/天，过剩 2.7 倍。
- **验收**：日产 ≤4 篇；`com.eric.xhswrite.plist` 注释说明依据
- **产出**：`scripts/xhs-loop/com.eric.xhswrite.plist`
- **依赖**：T5（先降单次成本，再定产能）

## T7 消耗 usage 的定时任务收敛到 10:00–19:00（2026-08-11 Eric 追加）
- [x] 完成 · `66075fc` · 三个任务全部落在窗口内，已 launchctl 重装并验证
- **要求**：所有消耗 usage 的定时任务只在白天 10:00–19:00 触发。
- **现状盘点**（已核实哪些真的调 claude）：
  | 任务 | 现时段 | 消耗 usage |
  |---|---|---|
  | com.eric.xhswrite | 04:30 · 14:30 | ✅ refine_loop |
  | com.eric.xhsaudit | 09:05 | ✅ independent_audit |
  | com.eric.xhsprobe | 02:30 · 08:30 · 14:30 · 20:30 | ✅ 内含 auto_analyze |
  | com.eric.xhsbrief | 21:00 | — 纯统计 |
  | com.eric.xhshealth | 09:30 · 19:30 | — 纯统计 |
  | com.eric.xhsdata | 21:15 | — 浏览器抓数 |
  | com.eric.xhspublish | 22:00 | — 浏览器发布 |
- **不动的**：brief / health / data / publish 都不消耗 usage；publish 的 22:00
  是小红书晚间流量时段，有业务依据，改到白天会掉曝光。
- **验收**：三个 usage 任务的所有触发点落在 [10:00, 19:00]；依赖顺序仍成立
  （probe 备料 → write 写稿 → audit 审核 → 22:00 发布）；launchd 重新加载后
  `launchctl list` 能看到，且 plist 里写明排期依据
- **产出**：`scripts/xhs-loop/com.eric.xhswrite.plist`、
  `scripts/xhs-health/com.eric.xhsaudit.plist`、`scripts/xhs-probe/com.eric.xhsprobe.plist`
- **依赖**：与 T6 一起做（时段和产能是同一个排期决策）

---

## 硬停止条件（满足任一立即停下报告）
- 同一个问题连续 2 次没修好 → ⛔ 停，不试第 3 次
- 需要删除/覆盖非本次生成的重要文件
- 需要 API Key 等外部凭证而项目里没有
- 台账与实际代码状态对不上

## 遗留 / 未解决
- 交互式开发会话 8 天 $895（最大单项），与自动化共用同一周额度。属流程外，
  已在测算文档记录，本台账不处理。


---

## 结论（2026-08-11 收工）

| | 改前 | 改后 | |
|---|---:|---:|---|
| 写稿单次 | $4.58 | $2.06 | −52% |
| 审核单次 | $2.99 | $1.13 | −62% |
| 写稿请求/次 | 24.4 | 2 | −92% |
| 审核 prompt | 121,578 字 | 24,557 字 | −80% |
| 日产 | 8 篇 | 4 篇 | 对齐配额 3 |
| **自动化周成本** | **$674** | **$189** | **−72%** |
| 含开发会话的周总计 | ~$1,569（撞顶） | ~$1,084 | 额度 $1,350–1,500 内，余量约 $266 |

判断：周额度不变的前提下，不会再出现整天停摆。撞顶时也不再空转一整天 ——
weekly limit 会立刻停手并打醒目提示，且任务都在白天，人能当场看到。

### 下一轮该看什么
- 跑满一周后复核实测值（改造后样本仍偏小：写稿 n=1、审核 n=2）
- 交互式开发会话 8 天 $895，是单项最大头，且与自动化共用同一周额度
- 若产能要上调，先看余量再动手，别直接加 plist 触发点
