# 选题拓宽与评论自动化 · 任务台账（2026-08-18 起）

**台账是进度的唯一真相。每完成一条：验证 → 提交 → 回写 `[x] + commit hash`。**

---

## 零、本轮要解决什么（2026-08-18 Eric 定）

### 决策记录

| # | 决策 | 依据 |
|---|---|---|
| E1 | 「场景地图 + 七种力」当**分类轴 + 配额轴**嵌进现有漏斗，不另起炉灶 | 见下方诊断：不缺词，缺分布 |
| E2 | 系列二「祝福 + 锦囊」**本轮就跑**，且给独立闸门（不过搜索位强度） | 它不吃搜索流量，走现有闸门一篇都出不来 |
| E3 | 评论**全自动**，三战场全开（自己笔记首评 / 读者回复 / 别人笔记评论） | Eric 在看过公告条文和 `4d78742` 限流前科后仍选此项，知情决策 |
| E4 | 模型分档：**评论链路 + 机修 + 标题档 → Sonnet 5**；写稿正文、返工、独立审核 → 保持 Opus 5 | 实测近 8 天 Claude 侧 100% 走 Opus（从不传 `--model`）；详见 token 二轮台账 T8 |

### 诊断：选题不丰富，卡点不在「想不出选题」

| 口径 | 实测（2026-08-18） |
|---|---|
| 词库总量 | 1107（候选 823 / 已验证 109 / 已发布 25） |
| 已验证**且没写过** | 68 个，其中搜索位强度达标可直接写 **26 个** |
| 已验证及以上 139 词的场景分布 | 面试/求职 **76（55%）** · 答辩述职 23 · 日常协作 12 · 试用期 7 · 谈薪 7 · 离职裁员 4 · 空降管理 2 · **绩效 1** |

对照场景地图六阶段：**冲突/危机 4 词、管理场景 2 词、绩效面谈 1 词**。
对照七种力：结构力/说服力有量，**示弱力、边界力、反馈力、化解力、破冰力几乎零对应词**。

根因是系统里**没有「场景」和「概念」这两个维度**。现有链路
`种子词 → 采集 → 词库（纯字符串）→ probe → pick_topic（按搜索位强度贪心）`
全程只认关键词字符串，于是：

1. **去重只能按字符串** —— 「HR问"你的缺点"该怎么回答」有 4 个变体词同时在已验证里，
   闸门「同词最多 2 篇」完全拦不住「同话题换个词串」。这就是感觉到的重复。
2. **没有覆盖度度量** —— 没有任何环节会说「边界力零产出」，偏斜可以一路滚下去。
3. **`pick_topic()` 是贪心的** —— 谁搜索位强写谁，而强的都在面试赛道（历史采集偏面试），
   它天然让偏斜自我强化。

`20260816-职场表达场景版图.md` 那次扩展（种子池 4→9）方向对，但只动了最上游的种子，
新词还堵在采集→验证的漏斗里，下游选词仍然只认「搜索位强度」这一把尺。

### 一个必须一起看的事实

已回填的搜索来源占比：3.3% / 1.3% / 1% / 2.6% / **45.3%** —— 除一篇外流量基本不是搜来的。
而 `MIN_SLOT_STRENGTH` 这把搜索位的尺，正在拦掉大量非面试新场景词（41 个已验证词根本没 probe 数据）。
**本轮把这把尺从「全局门槛」降级为「格子内排序」，就是冲着这个事实去的。**

---

## S 组 · 选题维度化

## S1 建两个单一事实源
- [x] 完成 · `场景地图.csv` 37 行 + `概念术语库.json` 7 概念，概念覆盖 7/7、匹配词无重复
- **改法**：
  - `xhs/素材库/场景地图.csv` —— 列：`阶段, 场景, 默认概念, 关键词模板, 周目标篇数`。
    内容来自 Eric 给的场景地图表（求职期 / 入职适应期 / 日常协作 / 关键节点 / 冲突危机 / 管理场景）。
  - `xhs/素材库/概念术语库.json` —— 直接用 Eric 给的那段 JSON（7 个概念的
    `name/definition/scenarios`），不改写、不意译。
- ⛔ **必须是数据文件不是 markdown**：这两份要被选题 / 写稿 / 机械校验 / brief 四处读。
  写成文档就会变成第五份各说各话的规格（D8 的老问题：正文字数在四处各写一份，模型挑了最宽松的那个）。
- **验收**：
  1. `python3 -c "import json;d=json.load(open('xhs/素材库/概念术语库.json'));assert len(d['concepts'])==7"`
  2. 场景地图 CSV 行数 = Eric 表里的场景总数，每行的「默认概念」都能在术语库里查到
- **产出**：`xhs/素材库/场景地图.csv`、`xhs/素材库/概念术语库.json`
- **依赖**：无

## S2 存量打标：词库 + 成稿
- [x] 完成 · `scripts/scene_map.py`。词库 947/1107 打标，**已验证及以上 139/139 = 100%**；
      成稿 140/140 打标（标签从词库派生，不往 200 多个 md 里各写一份）

### 打标基线（本组的对照起点，两周后拿它比）
| 概念 | 成稿数 | | 概念 | 成稿数 |
|---|---:|---|---|---:|
| 结构力 | 105 | | 化解力 | 10 |
| 说服力 | 58 | | 破冰力 | 5 |
| 反馈力 | 13 | | 边界力 | 3 |
| | | | 示弱力 | **2** |

**零产出场景 15/37**：技术面试 · 缺点类面试题 · 比价offer · 拒offer话术 · 向前辈请教 ·
拒绝不合理任务 · 给同事负面反馈 · 会议发言抢话权 · 加薪谈判 · 甩锅应对 · 道歉认错的分寸 ·
离职谈判 · 布置任务 · 跨级汇报 · 上台发言紧张

### 场景地图是被未打标词反推着改出来的（方法比结论重要）
第一版 33 行只能覆盖 81.3%。**没打上标的那 26 条词直接指出了表里缺什么**：
- 「空降新老板前30天」「新领导空降老员工如何应对」→ 缺**空降与新领导磨合**（账号已发 3 篇的场景，表里居然没有）
- 9 条 offer 催进度 / 等结果的词 → 缺**面试后等结果与催进度**
- 「编造工作经历和社保记录对不上」→ 缺**背调与履历核查**
- 「职场万用术」「结构化表达」「一开口显得能力强」→ 缺**日常表达力通识**

还查出一处误匹配：跨级汇报的匹配词写了「总经理|大老板|一把手|大领导」，
会把「总经理终面问期望薪资」这类**面试**词抢进管理场景。收窄成「跨级汇报|越级汇报|跨级|越级」。

⛔ 以后新增场景优先用这个办法反推，别拍脑袋加行 —— 未打标词是现成的、有搜索量背书的缺口清单。
- **改法**：词库.csv 加三列 `阶段 / 场景 / 概念`；成稿 md 头部加 `系列 / 场景 / 概念`。
  存量 1107 词先用规则匹配（关键词 → 场景正则），匹配不上的批量走 Gemini Flash 兜底（免费层，
  这类打标正是它已经在干的活）。成稿也要回填，否则算不出**已产出分布**。
- ⛔ 概念字段最多 2 个、`/` 分隔；措辞必须与术语库逐字一致（近义词一律视为未打标，不许「展示脆弱的能力」这种意译）。
- **验收**：
  1. 已验证及以上 139 词的打标覆盖率 ≥ 95%，未覆盖的逐条列出原因
  2. 打标后重算场景分布，与诊断表对得上（面试类应仍在 50% 上下 —— 对不上说明打标器有偏）
  3. 成稿标签能反算出「七种力 × 场景」矩阵，空格子数量与预期一致
- **产出**：`xhs/素材库/词库.csv`、成稿 md 头部、一次性打标脚本
- **依赖**：S1

## S3 `pick_topic()` 改配额驱动 + 去重升级
- [ ] 待办
- **改法**（`scripts/xhs-loop/refine_loop.py`）：
  ```
  ① 算每个（场景 × 概念）格子近 14 天产出数 vs 周目标
  ② 缺口最大的格子优先
  ③ 格子内再按现有搜索位强度排序挑词（强度降级为格子内排序，不再是全局门槛）
  ④ 格子里没有可用词 → 写一条「缺词信号」进台账，交给 S4
  ⑤ 所有格子都无词 → 退回现有全局最强逻辑
  ```
  去重同步升级：现在是「同关键词最多 2 篇」，改成「同（场景 × 概念）近 14 天最多 N 篇」。
- ⛔ **第 ⑤ 条不能省**。`pick_topic` 的注释里已经记着两次「把自己饿死」的前科
  （初版把竞争密度当硬条件、08-14 那次强度硬拦），配额是软约束，不许变成新的死闸。
- **验收**：
  1. 连续 5 次调用 `pick_topic()`，选出的词落在 ≥3 个不同（场景 × 概念）格子
  2. 构造用例：某格子已达上限 → 该格子的词不再被选中，且不报「无题可做」
  3. 全部格子无词的极端用例 → 仍能返回一个词并打印告警，不返回 None
- **产出**：`scripts/xhs-loop/refine_loop.py`
- **依赖**：S2

## S4 采集种子词由缺口驱动
- [ ] 待办
- **改法**：`daily_collect.pick_keywords()` 在现有轮换基础上，读 S3 写出的「缺词信号」，
  把缺口场景经「关键词模板」翻成定向种子词优先投放。
- ⛔ 保留现有的「按最近运行升序轮换」（`20260816` 那次修的 bug，种子无序切片导致新种子永不投放）。
  缺口驱动是**加权**，不是替换排序 —— 否则同一个空格子会被反复投到饱和。
- **验收**：
  1. 人为制造一个空格子（如「边界力 × 拒绝不合理任务」），下一轮 `daily_collect` 的投放清单里出现对应种子
  2. 该轮命中率记进关键词池；连续 3 轮命中率 <40% 的定向种子自动降权（沿用泛词串场的既有教训）
- **产出**：`scripts/xhs-collect/daily_collect.py`
- **依赖**：S3

## S5 brief 报覆盖矩阵
- [ ] 待办
- **改法**：`nightly_brief.py` 加一节「七种力 × 场景产出矩阵」+ 空格子告警。
- **这是整个 S 组唯一的验收信号**：两周后矩阵还是面试独大，说明配额没生效，回来重新设计。
- **验收**：brief 里出现矩阵；空格子数 > 阈值时打醒目提示；耗时增加 < 3s
- **产出**：`scripts/xhs-health/nightly_brief.py`
- **依赖**：S2

---

## X 组 · 系列二「祝福 + 锦囊」

## X1 新增 lane =「祝福流」
- [ ] 待办
- **改法**：`refine_loop` 已有 `lane` 参数（现值「搜索流」），扩一条「祝福流」：
  选题直接从场景地图轮换，**不要求词库「已验证」，不过搜索位强度闸门**。
- **验收**：`--lane 祝福流` 能在词库零可用词的情况下正常选出场景并出稿
- **产出**：`scripts/xhs-loop/refine_loop.py`
- **依赖**：S1

## X2 写稿 prompt + 独立审核口径
- [ ] 待办
- **改法**：
  - 写稿：固定开头识别符（「刷到这条，祝你……」）+ 祝福 + 一个具体场景的**可照抄**锦囊 + 概念点名
  - 审核：**不看**搜索意图 / 标题关键词密度；改判 ① 识别符句式在不在 ② 锦囊是否具体可照抄
    ③ 概念点名措辞是否一致 ④ 概念数 ≤2 ⑤ 有无编造
- ⛔ 不许复用搜索流的评分卡再「酌情减分」——那等于让祝福流稿永远拿不到及格分，
  重演「用推荐流口径审搜索流稿」那次的错（D11）。口径要独立成表，且在 `审核记录.csv` 的「口径」列标出来。
- **验收**：3 篇祝福流稿在新口径下都能拿到分且分档合理；同一批稿用搜索流口径复审应显著更低（证明口径确实分开了）
- **产出**：`refine_loop.build_prompt`、`independent_audit.py`、审核记录「口径」列
- **依赖**：X1

## X3 配比与库存目标
- [ ] 待办
- **默认**：每周 2 篇祝福流，其余走搜索流。写进 `refine_loop` 的库存目标（已有该机制，2026-08-16 加）。
- **验收**：连跑一周，祝福流产出 2±1 篇，且不挤占搜索流的库存目标
- **依赖**：X2

---

## G 组 · 七种力写稿与审核规则落地

⛔ **一条规则要同时落在 prompt 和机械校验里，只写进 prompt 等于没写。**
这是 D7 定过的：自评无处置权，判断权必须在代码手里。

## G1 写稿 prompt 注入
- [ ] 待办
- **改法**：`build_prompt` 注入选定的场景 + 概念 + 术语库定义；要求正文结尾**显性点名**概念
  （句式参考「这背后其实是____的问题」/「本质上考验的是____」）；单篇概念 ≤2。
- ⚠️ 术语库正文是**每篇都一样**的内容 → 必须落在 `SPLIT_MARK` **之前**（静态段），
  否则每篇多付一次 2× 写入缓存。改完跑 `python3 scripts/test_headless_split.py` 验静态段逐字节稳定。
- **验收**：test_headless_split 全过；生成 3 篇稿，结尾都出现术语库原词
- **依赖**：S1

## G2 `draft_check.py` 机械校验（本组的核心）
- [ ] 待办
- **新增五条**：
  | 检查 | 判据 |
  |---|---|
  | 术语措辞一致 | 出现近义词/意译（如「展示脆弱的能力」）→ 拦 |
  | 概念数 ≤2 | 正文出现的术语库原词去重计数 >2 → 拦 |
  | 结尾显性点名 | 正文末 N 字内必须出现至少 1 个术语库原词 |
  | 四段式 | 稿子标注了「基于读者原话」时，检查情境/诊断/解法/结果四段标记 |
  | 结果不得编造 | 「结果」段必须标注真实反馈 or 推理型（「大概率会……因为……」）二选一 |
- **验收**：对历史成稿跑一遍，逐条统计触发率；人工抽查 5 条触发是否都是真问题（误报率 0 才能上闸门）
- **产出**：`scripts/xhs-health/draft_check.py`
- **依赖**：S1

## G3 独立审核加 Eric 第五节那 5 条检查项
- [ ] 待办
- **改法**：`independent_audit.py` 加 5 条，任一不满足 → 打回重写并**指名缺失项**（不许只给总分）。
- ⚠️ 审核 prompt 是 T2 拆分过静态/动态的，新增内容属静态段，同样受「逐字节稳定」约束。
- **验收**：8 篇历史稿平行复审，新增检查项的判定与人工判断一致 ≥7/8；总分中位差 ≤2 且无稿子跨过 85 线
  （沿用 T2 的验收方法，理由相同：改评分标准就是改发布决策）
- **依赖**：G2

---

## C 组 · 评论全自动

### R1 风险声明（写在动手之前，不删）

`scripts/xhs-comment/draft_comments.py` 顶部记录的查证事实**继续有效**：

- 小红书 2026-03-10《关于打击AI托管运营账号的治理公告》：禁止利用技术手段模拟真人进行
  非真实内容创作或虚假互动，对 AI 托管注册/发布/互动的账号予以**封禁**
- 2026-06-10《关于规范搜索及问答生态相关行为的公告》：禁止「评论区配合、账号矩阵联动」
  制造口碑，处置为降权、下架、封禁
- 本账号已有前科：`4d78742`「主站今天被限，请求特征太像机器」

**Eric 在看过以上全部内容后，于 2026-08-18 决定改为全自动（E3）。**
那段注释不删、不改写成「已解除」，改成「已知风险 + 08-18 决策 + 风控措施」。
理由：把查证事实删掉，下一个人（包括未来的我）会以为这条线从来没有风险。

风险由 C4 的风控层和熔断承担，**压不到零**。最坏情况是账号级封禁。

## C0 前置：Eric 亲自扫码登录 www 主站（唯一需要人做的事）
- [ ] 待办 · **卡住 C2/C3/C4 全部**
- 在 cdp-proxy 控制的那个 Chrome 里登录一次 `www.xiaohongshu.com`。
  创作后台（creator.）的登录态**不通用**，且只给评论数量、不给评论正文（已验证）。
- **验收**：`python3 scripts/xhs-comment/draft_comments.py watch --probe` 不再打印 LOGIN_HINT
- ⛔ 我不替 Eric 登录。

## C1 评论链路模型分档（Sonnet 5）
- [x] 完成 · 见 token 二轮台账 T8。Sonnet 两次把「选项」写成回报，补 prompt 后 3/3 通过；
      另加 `normalize_punct()` 机械归一化半角标点
- **改法**：`draft_comments.py` 的 `run_claude` 改走 `headless_cli.build_argv(..., model="claude-sonnet-5")`。
  首评/回复/外部评论三条全部默认 Sonnet。
- **依据**：近 8 天首评链路 $35.01 / 251 次请求全在 Opus 上；本轮评论量会放大一个数量级
  （每天 12 条外部 + 首评 + 读者回复），继续挂 Opus 一个月就是几百刀。
- ⚠️ **Sonnet 的已知缺陷是格式遵循**（token 总纲结论 6：Sonnet + effort medium 在审核 CSV 上
  静默多插一列，且能通过「逗号 ≥14」的解析判据）。评论链路的输出格式简单（首评是纯文本、
  回复是严格三行），但验收必须显式查格式，不能只看内容像不像话。
- **验收**：见 token 二轮台账 T8 的 A/B 方案（同 5 条评论，Opus vs Sonnet 各跑一遍，
  比对「是否引用了对方的具体细节」「术语措辞一致」「行数/字数格式」三项）
- **依赖**：token 台账 T8

## C2 首评自动发（自己笔记）
- [ ] 待办
- **改法**：发布成功后 → 生成首评 → 自动发到该笔记下。**随机延迟 3–15 分钟**，
  不做「发布即评论」这种机器特征。
- **验收**：连续 3 篇发布后首评都成功出现在笔记下；台账状态正确；延迟落在区间内
- **依赖**：C0、C1

## C3 打通 `watch`：读者评论自动回复
- [ ] 待办
- **改法**：先跑 `watch --probe` 在**真页面**上定选择器，再填 `COMMENT_SELECTORS`，
  然后接自动回复（每条评论只回一次）。
- ⛔ 脚本注释里已经写死这条纪律：不先 probe 就凭空写选择器 = 抓到 0 条和「本来就没有新评论」
  长得一模一样，这正是这条链路反复出问题的方式。
- ⚠️ 原料本来就少：18 篇 1992 观看只有 6 条评论。这条线短期产出有限，
  真正的量在 C4 —— 但 C3 的风险低得多，**先做 C3 再做 C4**，用它验证发送链路和存活校验。
- **验收**：`--probe` 导出的结构能定位到评论正文 + 评论者 + 评论ID；抓取数与页面人工计数一致；
  回复发出后 1 小时仍存活
- **依赖**：C0

## C4 外部评论 + 风控层（风险最高，放最后）
- [ ] 待办
- **料源**：`xhs/素材库/评论区原话.csv`（584 条，带来源链接）
- **内容规则**（Eric 规则文档第四节）：针对对方的具体问题给可用建议；单条最多 1 个术语；
  不引流、不带主页跳转；不同时段刷同质文本
- **风控层（硬性，不是可选项）**：
  | 项 | 参数 |
  |---|---|
  | 每日上限 | 12 条 |
  | 单条间隔 | 随机 3–20 分钟 |
  | 时段 | 避开 0–7 点 |
  | 去同质 | 与已发评论做 n-gram 相似度，>0.6 拒发 |
  | 存活校验 | 发出后 1 小时回查是否还在 |
  | 熔断 | 存活率跌破阈值 / 出验证码 / 请求失败率异常 → 停 24h + brief 顶部告警 |
- **存活校验是最重要的一条**：被折叠或删除是最早的风控信号，比封号早。
- **验收**：
  1. 干跑模式下生成 12 条，人工抽查 5 条都真的针对了对方那条评论（不是通用模板）
  2. 相似度拒发在构造用例上生效
  3. 真发第一天限 3 条，24 小时后存活率 100% 才放开到 12
  4. 熔断能被手动触发并真的停 24h
- **依赖**：C3

## C5 评论台账 + brief 一节
- [ ] 待办
- `xhs/素材库/评论台账.csv`（脚本里已定义列名但**文件从未生成过**）：
  日期 / 战场（首评·回复·外部）/ 笔记链接 / 对方原话 / 发出内容 / 概念 / 状态 / 存活校验
- brief 加一节：评论产出 · 存活率 · 风控事件
- **验收**：三条战场的记录都能落进同一张表；brief 能读出存活率
- **依赖**：C2

## C6 评论反哺选题
- [ ] 待办
- 评论里撞见的高频处境 → 写成「缺词信号」喂给 S3 的格子缺口，同时作为 G2 四段式案例的输入源。
- **验收**：一周内至少有 1 个选题的来源可追溯到评论区原话
- **依赖**：C5、S3

---

## 执行顺序

1. **S1 → S2 → S3 → S5**（选题主线，风险最低、收益最直接，且不依赖任何人工动作）
2. **G1 → G2 → G3**（规则落地，S 组打完标才有意义）
3. **X1 → X2 → X3**（祝福流）
4. **C0（Eric 做）→ C1 → C2 → C3 → C4 → C5 → C6**

C0 现在就可以做，做完 C 组随时能插进来。

## 硬停止条件

- 同一个问题连续 2 次没修好 → ⛔ 停，不试第 3 次
- G3 的平行复审出现「有稿子跨过 85 线」→ ⛔ 停，不上生产，回来重新设计验收
- C4 存活率跌破阈值 → ⛔ 熔断，停 24h，不要「再试一次看看」
- 台账与实际代码状态对不上 → 停下来核对
