# 全自动补齐 · 台账（2026-08-13）

> 来源：Eric 2026-08-13 深夜的 7 项指示。**本文件是进度的唯一真相**，对话不是。
> 全部 8 条已完成。遗留问题在最后一节。

## 背景：一条被推翻的结论

独立审核把《答辩被打断，评委问的根本不是项目本身》判 63 红 → 归档，
理由「搜索位人群主体是毕业答辩学生，成稿答的是职场晋级答辩，属相邻问题错配」。

**Eric 明确推翻：稿子没问题，错的是我。** 整个账号面向职场，
「毕业生答辩」这类词本来就不该进关键词池 —— 错配的根不在成稿，在选词。
所以修法分两层：这篇照发（T1），选词层与审核判据都补上「账号定位」这个前提（T2）。

---

## 任务

- [x] **T1 发布《答辩被打断，评委问的根本不是项目本身》** — 已定时 2026-08-14 20:00 [ced26b9]
      放行后接连撞出三个 bug，全不在稿子上：
      ① `draft_check` 的 NOT_BUT/GENERIC_READER 量的是全文而非正文节 → 备选标题、
         打分说明、连「机械自检」那行自己都被算进去（该行字面写着「「不是X是Y」句式 1 处」，
         自我命中）。正文节实为 1 处，合格。与 08-12 修的 CTA 窗口同型，那次漏修这两条。
      ② `auto_publish.gate` 取整份 stdout 第一条「-」行当拦点 → 报的是**别的稿**的
         「正文节 899 字」。改成 `--file` 只跑本篇。
      ③ `set_schedule` 跨午夜必废：目标日 == 已选日时点它是**取消**，组件连带把定时开关
         重置、datepicker 从 DOM 消失 →「打开日历：没找到元素」。22:00 跑撞不上，23:25 跑必中。

- [x] **T2 账号定位＝职场，写进选词层与审核判据** [e4a02eb]
      `probe.load_pending` 加 `is_off_topic()`：黑名单（校园/泛社交/泛表达训练）∩ 非白名单
      （职场信号词）才挡，规则写死不问模型。实测 442 词队列精准挡下 4 个
      （爱上和你聊天／新闻复述法／建立主体性／锻炼输出表达），
      「应届生怎么谈薪」「大学生面试技巧」「校招面试问题」等求职词全部放行 ——
      求职是职场入口，不是校园话题。被挡的词打印出来，不静默丢词。
      `eric-xhs-audit/SKILL.md` 维度 1 补上前提：不得以「搜索位人群不是职场人」扣分。

- [x] **T3 launchd 日志迁出 /tmp** [0e7e5b1]
      改到 `~/Library/Logs/xhs/`，按天切分、留 30 天。放本机盘而非仓库 `logs/`：
      仓库在外置卷上，卷没挂载时 launchd 连日志文件都打不开，那就退回「失败了但没有
      任何记录」的老问题。`install_agents.py` 的模板一并改 —— 它是权威来源。
      验收：kickstart 后 `xhs-probe-daily_probe-2026-08-13.log` 有完整 stdout+退出码。

- [x] **T4 opencli 彻底修复（TCC 同款收敛）** [0e7e5b1]
      根因：`probe_opencli.py:52` 用裸名调 `opencli`，launchd 的 PATH 只有
      `/usr/bin:/bin:/usr/sbin:/sbin`。与 08-05 的 TCC 坑同型：**launchd 环境 ≠ shell 环境**。
      两层防御：runner 统一给子进程补 PATH（覆盖所有定时任务）＋ 脚本自身
      `shutil.which` + 常见位置兜底（直接调用也不踩）。找不到就抛，不再静默返回 None。
      验收：`env -i PATH=/usr/bin:/bin` 下解析到 `/opt/homebrew/bin/opencli`；
      kickstart 实跑 **成功 5/6、告警=否**，对比修复前三轮 0/5、0/6、0/6 全 failed。

- [x] **T5 退出码语义化 —— 「跑挂了」与「跑通但有问题」分开** [0e7e5b1]
      退出码 1 原来不是 bug，是「发现了告警」，但 brief 一律画成 ❌ 静默失败。
      监控自己天天挂红，人会对红色脱敏，真正挂掉的任务反而混在里面看不见。
      改为 0=无告警 / 1=检查本身失败 / 3=跑通但有告警（3 避开 runner 占用的 2），
      brief 认 3 为 ⚠️。`daily_probe.sh` 探测失败只 echo 不 exit 的洞一并堵上 ——
      正是它让全挂的 probe 报成 ✅。验收：实测退出码 3。

- [x] **T6 数据回填链路审计 + 失败可见** [4ff77fe]
      结论：**链路是通的，今天 21:15 那次没做完**。手动实跑补上了
      2 条笔记链接（含今晚发的那篇）、2 条满 7 天数据、1 条搜索来源占比、预测复盘归因。
      `daily_data.sh` 三步的 `|| echo 继续` 与 daily_probe 同病，改成如实 exit 1。
      顺带：两个 .sh 硬编码 `PY=/usr/bin/python3`（3.9.6）而 runner 是 3.14.6，
      改由 runner 注入 `XHS_PY` 统一。验收：kickstart 实跑退出码 0，日志完整。

- [x] **T7 笔记链接告警改成「发布满 7 天」** [0e7e5b1]
      ⚠️ 只推迟「报」，没有推迟「补」：后台列表页只显示最近几天，等满 7 天再去捞就
      **永远捞不到**；而 `aged_candidates` 反过来又要靠链接才能开详情页（fetch_stats.py:173），
      补晚了整条预测闭环断死。补要趁早，报要延后。
      验收：告警从 6 条降到 1 条（只剩真正满 7 天那个）。

- [x] **T8 待人工分诊 + 话术沉淀** [4ff77fe]
      新增 `triage_pending.py`，接管「值不值得再投入」这个判断（Eric 授权）。
      审核方写「自动分诊」不冒用「独立审核」—— 后者会污染 calibrate_audit 的样本。
      分数红线沿用原结论不重新打分：两套分数并存则校准无从对账。
      实测 11 篇全判返工（理由都落在定点可修），**待人工归零**，返工队列 40→51。
      新增 `话术复用库.csv`（话术／记忆句／框架），捞出 32 条、32/32 带关键词溯源。
      已挂 `com.eric.xhstriage` 每天 20:30，走 Gemini 免费层，零 Claude 额度。

---

## 第二批（Eric 2026-08-14 追加）

- [x] **T9 数据回填/复盘归因之后，实际行动是什么** [c6839e9]
      调查结论（2026-08-14）：**目前是零行动**，两条链路都断在「打印出来」这一步。
      ① 复盘 → predict：结论写进 预测校准.csv，predict.py 预测时把偏差**打印**到
         refine_loop 的日志里。没人看那个日志。且复盘自己立规矩「≥5 篇才准改系数」，
         低密度独立样本只有 3 篇 → 永远停在提示。
         而这 3 篇的倍数是 **0.06 / 0.24 / 0.42** —— 不是系数偏了，是量级错了。
      ② 搜索来源占比 → **完全没有下游**。健康检查只管「满 7 天没回填就催」，
         回填之后没有任何脚本读它。而它是 L3 **唯一主指标**。
         已回填 3 条：3.3% / 1% / 1.3% —— 搜索几乎没带来量。
         整条搜索流策略成不成立，答案就在这三个数字里，但没人被告知。
      已做：① brief 新增「📊 数据说了什么」一节，报数字 + 逼出的决定；
      ② **执行**了复盘早就写出、一直没人做的那条改法（VIEWS_BASE 低密度按
      median_likes 拆档），med 本来就算好了只是没传进 predict。
      ⚠️ 回测三篇只修好 1/3 —— 另两篇说明 median_likes 与观看看不出单调关系，
      而 VIEWS_BASE 整张表都建在「前排赞高＝盘子大」这个假设上。这条没解决，等样本。

- [x] **T10 链接回填告警改成「第二天」**（推翻昨天定的 7 天）[c6839e9]
      Eric 定。第二天笔记确实已经发出去、列表页也抓得到，没链接就是异常，
      压到 7 天等于让问题多藏 6 天。回填动作本来就每天跑，这里只改告警门槛。
      验收：health_check 对「发布日 < 今天且无链接」的词告警。

- [x] **T11 分诊更敢判归档** [c6839e9]
      11 篇全判返工，一篇归档都没有，偏保守。返工队列已 51 篇而写稿每天只消化 2 篇。
      已做：硬信号 A/B/C + 「拿不准判归档」的默认倾向 + 把分数序列喂给模型；
      新增 --recheck-stuck 回头清返工队列。实跑 4 篇全判归档
      （含审了 12 轮仍在原地的《2026-08-07_汇报被打断》），另捞 8 条素材。

---

## 第三批（Eric 2026-08-14：把采集也做成定时任务）

**背景**：`run1-run4` 那四轮采集**从来不是自动的**。`.run3_work/` 里的
`append_runlog.py` / `update_pool.py` / `update_kudb.py` 是上一个会话临时写的一次性脚本，
`backfill.py` 注释里写着「运行日志由采集任务独占」，但仓库里根本没有那个采集任务。
这是全自动链条上最上游、也是最后一个真空洞 —— 不补的话每天的选题供给都要人开一次会话。

手工流程（从运行日志备注与临时脚本还原）：
1. 选 8 词 = 4 固定种子 + 2 长空档活跃复查 + 2 候选首投/复投
2. 每词搜 20 条，取 标题/URL/点赞/发布日
3. 相关性过滤（剔非职场内容，run3 那轮「高情商破局」16 条命中里剔掉了 10 条）
4. 与记忆库去重，算命中率
5. 入记忆库、更关键词池（运行次数/命中率/最近运行/平均热度）、命中率 ≥40% 的候选升活跃
6. 写运行日志.csv

- [x] **T12 采集自动化** — `scripts/xhs-collect/daily_collect.py`，每天 08/12/16/19 点
      能力边界：opencli 有 search（字段 rank/author/likes/title/url/published_at，
      与记忆库列完全对得上）、comments、note；**没有**下拉词/相关搜索命令。
      去重键：note_id 优先、回退标题（记忆库 4560 行里只有 2279 行的 URL 带笔记 ID）。

      选词沿用手工流程验证过的配比：4 固定种子 + 2 长空档活跃 + 2 候选。
      「长空档」不是随便挑 —— 08-13 run3 实测长空档词命中 30.3%/60.0%，
      而当天第 3 次复查的种子词只有 30%/5%/0%/15%，所以按「最近运行」升序取最久没跑的。

      只有「这条笔记算不算职场内容」交给模型（Gemini 免费层），其余全是代码算的。
      这一项非交给模型不可：run3 那轮「高情商破局」原始命中 16/20，
      人工剔掉 10 条泛人性心理内容后只剩 6 条，关键词匹配做不到。
      首轮实跑验证了这一点：「如何自信表达」抓 19 条剔掉 9 条。

      实跑：8 词 / 抓 152 条 / 新增 17 条，记忆库 4560 → 4577，
      关键词池运行次数与最近运行已更新，运行日志新增 run1 行。

      **两处刻意没做**（别当成遗漏）：
      · 新词收割 —— 手工流程的新词来自搜索页「大家都在搜/相关搜索/筛选标签」，
        opencli 无此命令。硬爬那些元素等于退回 DOM 选择器，站点一改版就废，
        正是当初迁到 opencli 要甩掉的东西。改从标题提炼，另开一步。
      · 评论区原话 —— 已由 probe 链路覆盖（probe_opencli 抓正文+评论 →
        harvest_cases 搬进案例库）。再抓一遍是重复劳动，还多烧一轮风控额度。

---

## 遗留 / 下一步

1. **返工队列 51 篇，而 xhswrite 每天只跑 2 批 × 1 篇** —— 按当前速度要一个月才能消化。
   待人工的堵点解开了，堵在了下一个环节。要么提高返工吞吐，要么让分诊更敢判归档
   （目前 11 篇无一归档，可能偏保守）。
2. **手工点定时开关两次失败**（dispatchEvent 与 Input.dispatchMouseEvent 各一次）。
   主链路重跑预填能绕开，没有阻塞，但旁路没查透。
3. **候选词积压 899 个**（阈值 200），需要一次退休清点。
4. **自评与独立审核处置冲突 7 例** —— 自评这一环基本没起作用，还在花 token，
   值得评估是否直接砍掉。
5. **审核校准样本 0/8** —— 还差 8 篇够龄样本才能反推审核标准。

## 硬停止条件（CLAUDE.md 第十四节）

- 同一个问题连续 2 次没修好 → 停下来问 Eric，不试第 3 次
- 台账与实际代码状态对不上 → 停下来核对
