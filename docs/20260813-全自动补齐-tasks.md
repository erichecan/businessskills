# 全自动补齐 · 台账（2026-08-13）

> 来源：Eric 2026-08-13 23:xx 的 7 项指示。
> **这份文件是进度的唯一真相**，对话不是。每完成一条回写 `[x] + commit hash`。
> 下一周期从**读这个文件**开始，不从「我记得做到哪」开始。

## 背景：一条被推翻的结论

独立审核把《答辩被打断，评委问的根本不是项目本身》判 63 红 → 归档，
理由「搜索位人群主体是毕业答辩学生，成稿答的是职场晋级答辩，属相邻问题错配」。

**Eric 明确推翻：稿子没问题，错的是我。** 整个账号面向职场，
「毕业生答辩」这类词本来就不该进关键词池 —— 错配的根不在成稿，在选词。
所以修法有两层：这篇照发（T1），选词层把学生向词清掉（T2）。

---

## 任务

- [ ] **T1 发布《答辩被打断，评委问的根本不是项目本身》**
      当前拦点两个：① 独立审核处置为「归档」→ 需 `--approve` 人工放行；
      ② 成品图目录 `成品图/2026-08-13_答辩被打断评委问的根本不是项目本身/` 是空的，
         闸门第 4 条要求至少 1 张已渲染 png。
      验收：`auto_publish.py --list` 显示该篇 ✅；实际发布成功并写入发布日志.csv
            （状态列为「✅ 全自动已定时」或已发布链接），不是 ⏸。
      产出：成品图 png、审核记录.csv 人工放行行、发布日志.csv 新行
      依赖：无

- [ ] **T2 关键词池清掉学生/毕业生向词（错配的根）**
      账号面向职场，学生向词进池就是浪费探测额度 + 制造错配。
      验收：关键词池.csv 与词库.csv 中「毕业答辩/学生/应届/校招/论文」类词被标退休；
            选词入口有一道显式过滤，新词再进不来（写死规则，不靠模型判断）。
      产出：过滤规则代码 + 一次性清点结果
      依赖：无

- [ ] **T3 launchd 日志迁出 /tmp**
      现状：7 个任务全部 StandardOutPath=/tmp/xhs*.log，今天这些文件**一个都不存在**，
      而任务确实跑了（产出文件 mtime 对得上）。今晚排查 probe 全靠翻 JSON 里的 _error。
      验收：日志落在仓库内 `logs/`（跟 .gitignore 一起处理），按天切分；
            重跑任一任务后能在 logs/ 里看到本次完整 stdout+stderr；重启后仍在。
      产出：7 个 plist + launchd_runner.py
      依赖：无（但先做，T4/T5 的验证都要靠它）

- [ ] **T4 opencli 彻底修复（TCC 同款收敛）**
      根因：`probe_opencli.py:52` 用裸名 `"opencli"` 调 subprocess，
      launchd 的 PATH 只有 `/usr/bin:/bin:/usr/sbin:/sbin`，
      而 opencli 在 `/opt/homebrew/bin/opencli`（软链 → node_modules/.../main.js）。
      手动跑必通、launchd 跑必挂 —— 与 2026-08-05 的 TCC 坑同型（launchd 环境 ≠ shell 环境）。
      **要的是彻底修**：不是给一个脚本补 PATH，而是像 launchd_runner 收敛 TCC 那样，
      在 runner 这一层把外部命令的解析口径统一，让后续任何脚本调外部 CLI 都不会再踩。
      验收：`launchctl kickstart` 触发 xhsprobe 后，探测日志.csv 新行成功数 > 0；
            探测原始 JSON 里不再出现 `No such file or directory: 'opencli'`；
            人为把 PATH 清空跑 daily_probe.sh 也能通。
      产出：launchd_runner.py / probe_opencli.py / daily_probe.sh
      依赖：T3（要看日志验证）

- [ ] **T5 健康检查退出码 1 —— 彻底修复**
      brief 报「❌ 健康检查 — 上次退出码 1」。监控自己坏了，别的绿灯都不可信。
      注意 daily_probe.sh:46-49 那种「失败只 echo、退出码不传播」的写法要一并查，
      正是它让今晚 brief 把全挂的 probe 报成 ✅。
      验收：手动跑 health_check.py 退出码 0；有真告警时退出码语义明确
            （区分「检查本身失败」与「检查通过但发现问题」，brief 能分开显示）。
      产出：health_check.py、nightly_brief.py、daily_probe.sh
      依赖：T3

- [ ] **T6 数据回填链路审计 —— 是否像采集一样独立跑**
      Eric 的问题：数据回填都做了吗？是不是像采集一样有独立流程？
      现状：回填挂在 daily_data.sh（21:15）里，三步串行 fetch_stats → review_prediction
      → harvest_cases，且每步 `|| echo 继续`，失败不传播。
      验收：逐项核对「哪些数据该回填、今天实际回填了没、失败是否被吞」，
            结论写进 docs/；该独立调度的拆出来，失败要能被 brief 看见。
      产出：审计结论文档 + 必要的调度调整
      依赖：T3、T5

- [ ] **T7 笔记链接回填改成「发布满 7 天再回填」**
      Eric 指定。现状 fetch_stats.py 的 backfill_note_links 每天扫、命中 16/23，
      剩下靠人工，健康告警里长期挂着 6 条「标已发布但无笔记链接」。
      验收：回填只针对发布满 7 天的笔记；不满 7 天的不再进告警名单
            （否则告警永远有噪音）；健康检查的判定口径同步改。
      产出：fetch_stats.py、health_check.py
      依赖：无

- [ ] **T8 「待人工」由我处置 + 素材沉淀（最大的一块）**
      Eric：待人工这里你自己看，不要我来看。质量不行就退回，
      **但关键词、原话、案例还是提炼出来**，以后能给其他文章用。
      两件事：
        ① 自动处置：待人工 → 我判定 → 退回返工 / 归档，不再堆积等人
        ② 沉淀：无论稿子生死，把里面的关键词 / 评论区原话 / 案例抽出来入库复用
      验收：今天积压的 5 篇「待人工」全部有终态；素材沉淀有落库证据
            （案例库.csv / 评论区原话.csv / 词库.csv 有新增行且可溯源到来源稿）；
            这条链路能被定时任务自己跑，不需要人点。
      产出：新脚本 + 审核记录.csv 终态回写 + 素材库增量
      依赖：T3

---

## 硬停止条件（第十四节）

- 同一个问题连续 2 次没修好 → 停下来问 Eric，不试第 3 次
- 需要动生产数据（词库/案例库批量改写）前先看清楚再动
- 台账与实际代码状态对不上 → 停下来核对
