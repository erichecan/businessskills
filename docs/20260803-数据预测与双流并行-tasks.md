# 数据预测与双流并行 · 任务台账

> 起于 2026-08-03 会话，承接 [20260802-成稿loop与素材自动化-tasks.md]。
> Eric 五条指令 → 六个任务。**台账是进度的唯一真相，对话不是。**

## Eric 的五条原话（不要改写、不要软化）

1. **不要人工把搜索位有空缺的标成已验证，自动标已验证。**
2. **待验证是什么逻辑，我个人觉得不需要待验证，成稿后发布，让市场去检验。**
3. **候选词积压 >200，你计划如何处理，我希望能尽快变成成稿。**
4. **成稿标准加数据预测**：联网搜小红书爆款技巧+官方规则 → 每篇成稿预测数据 →
   拿到回填数据后自己 review「当时为什么没预测准、怎么能更准」，含评论/转发/收藏。
5. **推荐流不能废弃**：推荐流不做视频，但按原成稿路线做图文，账号内容丰富度才起得来。

## 现状事实（已核实，不要重新推导）

- `backfill.py` 已有 `DISPOSITION_TO_STATUS = {"做":"已验证","缓":"候选","放弃":"放弃"}`，
  **自动标已验证的机制已经存在**，缺的只是第 2 层判断的 headless 化——
  现在 `eric-xhs-probe` skill 要在对话里跑才产出 `probe_*.result.json`。
- 三层架构：`probe.py`（客观提取，已自动）→ skill（判断，**手动**）→ `backfill.py`（回填，已自动）。
- `judge_density()` 是确定性规则（v3 阈值，15 个实测样本标定），skill 不得推翻。
- 词库现状：91 行，已验证 9（7 已写）、候选 23+、待验证 6、放弃 24。关键词池候选积压 **666**。
- `发布数据.csv` 已有真实回填：晋升答辩 观看262/赞9/评0/藏5/转1、第三轮hr 观看129/赞3/评0/藏0/转1
  （均发布 0 天，数据太新，7 天后才有参考价值）。
- `eric-xhs-audit` SKILL.md 第 205–218 行已有**推荐流口径附录**（默认不启用），含维度对照表。
- CES 加权公式已在 skill 第 117 行：赞×1+藏×1+评×4+转×4+关注×8。

## 任务

- [x] T1 探词全自动闭环：probe → headless 分析 → backfill 自动标已验证
      验收：一条命令跑完三层，词库里出现新的「已验证」行且无人工介入；
            第 2 层用独立 claude -p 进程（不在对话里），产出 probe_*.result.json；
            density.verdict 仍由脚本决定，模型不得推翻（skill 既有约束）
      产出：scripts/xhs-probe/auto_analyze.py、串联脚本或 plist 调整
      依赖：无
      完成：10 个探测结果全自动分析 → backfill 自动定级。**8 个词自动标「已验证」**
            （3 个从「待验证」升级、3 个从「放弃」翻案、2 个从「候选」升级），
            另挖出 78 个新长尾候选词、6 条原话。词库 91 → 186 行，已验证 9 → 23 个。
            density_echo 与脚本 verdict 不一致时以脚本为准（代码校正，不让模型改判断）。
            「总经理终面」密度=高 仍判「做」——符合 skill「密度高但空缺明显仍可做」。

- [x] T2 取消「待验证」中间态
      验收：词库状态收敛为 候选/已验证/放弃/已成稿/已发布；现有 6 个「待验证」行按
            density 重新落位；backfill.py 的 PLACEHOLDER 与 skill 文档同步；
            refine_loop 选题不再依赖人工升级
      产出：xhs/素材库/词库.csv、scripts/xhs-probe/backfill.py、相关 skill 文档
      依赖：T1（自动定级先能跑）
      完成：状态机收敛为 候选/已验证/放弃/已发布（待验证残留 0）。3 个未探测的
            「待验证」迁回「候选」；backfill 新词入库状态改「候选」。
            **顺带挖出一个卡住 T3 的真 bug**：probe.load_pending 精确匹配「候选」，
            而历史数据有 26 行写成「候选-源自记忆库(热度10000)」，这些词永远排不进
            探测队列。改 startswith 后待探测队列从取不到 → 117 个。

- [ ] T3 候选积压消化提速（666 → 尽快成稿）
      验收：给出并实施吞吐方案，算清每天能探多少词、能出多少稿；
            探测 limit 与频次调整落到 plist；refine_loop 支持一次跑多篇
      产出：plist、refine_loop.py --count 参数
      依赖：T1、T2

- [ ] T4 成稿数据预测
      验收：联网调研小红书爆款/官方规则并落 docs/；成稿时输出预测值
            （观看/点赞/收藏/评论/转发 + CES）写入 预测记录.csv；
            预测规则可解释（基于 density、词意图强度、历史同类稿实际数据），不是拍脑袋
      产出：docs/20260803-小红书数据预测调研.md、xhs/素材库/预测记录.csv、
            scripts/xhs-loop/predict.py、eric-xhs-audit SKILL.md 加预测维度
      依赖：无（可与 T1–T3 并行）

- [ ] T5 预测复盘闭环
      验收：发布满 7 天数据回填后，自动比对预测 vs 实际，产出偏差分析
            「当时为什么没预测准 / 怎么能更准」，并把结论写回预测规则；
            health_check 加一条：有可复盘但未复盘的笔记 → 告警
      产出：scripts/xhs-loop/review_prediction.py、xhs/素材库/预测复盘.md
      依赖：T4

- [ ] T6 推荐流并行（图文，不做视频）
      验收：refine_loop 支持 --lane search|recommend；推荐流走 audit skill 附录口径
            （标题留悬念/首图0.3秒停留/CES 为结论依据）；两条线产出可区分、
            审核记录「口径」列正确落值；定一个产出配比并说明理由
      产出：scripts/xhs-loop/refine_loop.py、eric-xhs-audit SKILL.md、词库或选题源标记
      依赖：T4（推荐流的预测口径与搜索流不同）

## 未解决问题

- 发布环节「定时开关点击未生效」，auto_publish 只到预填，定时+发布仍全人工。
  会拖住整条链的下游——稿子产得再多，发不出去就拿不到回填数据，T5 就没有燃料。
- 发布数据目前只有 2 篇且都是发布 0 天，样本量不足以标定预测规则；
  T4 的初版规则只能基于公开方法论 + density，等 7 天后用真实数据校准。
